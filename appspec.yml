version: 0.0
os: linux

files:
    - source: /
      destination: /home/ec2-user/app/deploy-temp

hooks:
    AfterInstall:
        - location: scripts/deploy.sh
          timeout: 600
          runas: ec2-user
